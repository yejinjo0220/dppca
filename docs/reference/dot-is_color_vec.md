# Check whether labels are valid colors

Internal helper used by group plotting functions to determine whether
group labels can themselves be interpreted as colors.

## Usage

``` r
.is_color_vec(x)
```

## Arguments

- x:

  Character vector.

## Value

Logical scalar.
