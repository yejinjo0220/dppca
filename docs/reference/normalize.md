# Normalize a vector to unit norm

Internal helper that normalizes a vector to have Euclidean norm 1.

## Usage

``` r
normalize(x)
```

## Arguments

- x:

  A numeric vector.

## Value

A numeric vector with Euclidean norm 1.
