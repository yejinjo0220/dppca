% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_pca.R
\name{summary.dp_pca}
\alias{summary.dp_pca}
\alias{print.summary.dp_pca}
\title{Summarize stored DP PCA estimates}
\usage{
\method{summary}{dp_pca}(object, ...)

\method{print}{summary.dp_pca}(x, digits = max(3L, getOption("digits") - 3L), ...)
}
\arguments{
\item{object}{A \code{\link[=dp_pca]{dp_pca()}} result.}

\item{...}{Additional arguments. The summary calculation takes no extra
options; print methods pass formatting arguments to the printed tables.}

\item{x}{A \code{summary.dp_pca} object.}

\item{digits}{Significant digits used when printing tables.}
}
\value{
A \code{summary.dp_pca} object. \code{importance} contains DP eigenvalues,
PVE and cumulative PVE, with a column for each estimated component.
\code{budget} contains the allocated eps and delta for loading, scree,
score frame and score histogram, plus the total. Other fields describe
data dimensions, selected axes, methods, preprocessing and whether a
non-private reference is stored. Raw scores and loading matrices are
not copied into the summary.
}
\description{
Summarizes an existing fit without new estimation or random sampling.
}
\details{
PVE is normalized over the \code{k} estimated eigenvalues. If \code{k} is smaller
than the number of variables, cumulative PVE of one does not mean that
the first \code{k} components explain all of the data's variance. If all
estimated eigenvalues are zero, PVE and cumulative PVE are shown as zero.

The budget table records component allocations, not measured privacy loss
or cumulative spending across calls. With external \code{V_dp}, the loading
row records its prior cost. The frame uses no delta; all scree delta is
used by the vector mechanism, including the Huber gradient iterations.
Non-private comparison values are not included in the importance table.
}
