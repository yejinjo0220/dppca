% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_lambda_huber.R
\name{dp_lambda_huber}
\alias{dp_lambda_huber}
\title{Differentially private eigenvalue estimation via Huber-type robust mean}
\usage{
dp_lambda_huber(
  X,
  k,
  eps_total,
  delta_total,
  dp_v_flag = FALSE,
  cpp.option = FALSE,
  center = TRUE,
  standardize = TRUE,
  mu0 = 0,
  eta0 = 1,
  T = NULL,
  M = NULL,
  k_min_star = -40L,
  k_max_star = 40L,
  include_zero_bin = TRUE,
  m2_frac = 1/4,
  step_schedule = c("fixed", "1/sqrt(t)"),
  mono = TRUE
)
}
\arguments{
\item{X}{Numeric data matrix with observations in rows.}

\item{k}{Number of leading components.}

\item{eps_total}{Total privacy budget epsilon.}

\item{delta_total}{Total privacy budget delta.}

\item{dp_v_flag}{Logical; whether to privatize the PCA basis.}

\item{cpp.option}{Logical passed to \code{mech_tau_sph()}.}

\item{center}{Logical; whether to center columns before PCA.}

\item{standardize}{Logical; whether to standardize columns before PCA.}

\item{mu0}{Initial point for the scalar noisy GD updates.}

\item{eta0}{Base step size for noisy GD.}

\item{T}{Number of GD iterations. If \code{NULL}, defaults to \code{ceiling(log(n))}.}

\item{M}{Number of blocks for the scale proxy routine. If \code{NULL},
defaults to \code{floor(sqrt(n/2))}.}

\item{k_min_star, k_max_star}{Integer range of dyadic histogram bins.}

\item{include_zero_bin}{Logical; whether to include a separate zero bin.}

\item{m2_frac}{Fraction of the eigenvalue budget allocated to the scale proxy.}

\item{step_schedule}{Either \code{"fixed"} or \code{"1/sqrt(t)"}.}

\item{mono}{Logical; whether to apply monotone post-processing.}
}
\value{
A list containing private eigenvalue estimates and auxiliary objects.
}
\description{
This function estimates leading PCA eigenvalues by first obtaining a PCA
basis, then applying a scalar robust private mean estimator to the projected
squared scores along each component.
}
