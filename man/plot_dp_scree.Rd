% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dp_scree.R
\name{plot_dp_scree}
\alias{plot_dp_scree}
\title{Plot differentially private scree curves}
\usage{
plot_dp_scree(
  X,
  k,
  dp_lambda = c("both", "clipped", "pmwm", "huber"),
  eps_total,
  delta_total,
  center = TRUE,
  standardize = TRUE,
  B_mult = 2,
  beta = 1.01,
  a = NULL,
  b = NULL,
  trim_const = 10,
  eta = 0.01,
  split_mode = TRUE,
  mu0 = 0,
  eta0 = 1,
  T = NULL,
  M = NULL,
  k_min_star = -40L,
  k_max_star = 40L,
  include_zero_bin = TRUE,
  m2_frac = 1/4,
  step_schedule = c("fixed", "1/sqrt(t)"),
  dp_v_flag = FALSE,
  cpp.option = FALSE,
  mono = TRUE,
  type = c("evr", "lambda"),
  show_nonprivate = TRUE,
  show_points = TRUE,
  col_map = NULL,
  lty_map = NULL,
  pch_map = NULL,
  main = NULL,
  xlab = "Component",
  ylab = NULL,
  ylim = NULL,
  legend_pos = "topright",
  ...
)
}
\arguments{
\item{X}{Numeric data matrix with observations in rows.}

\item{k}{Number of leading components.}

\item{dp_lambda}{Which DP estimator(s) to plot. One of
\code{"both"}, \code{"clipped"}, \code{"pmwm"}, or \code{"huber"}.}

\item{eps_total}{Total privacy budget epsilon.}

\item{delta_total}{Total privacy budget delta.}

\item{center}{Logical; whether to center columns before PCA.}

\item{standardize}{Logical; whether to standardize columns before PCA.}

\item{B_mult}{Clipping multiplier for the clipped estimator.}

\item{beta}{Log-binning base for PMWM.}

\item{a, b}{Support bounds for PMWM.}

\item{trim_const, eta}{PMWM trimming parameters.}

\item{split_mode}{Logical; whether PMWM splits the sample.}

\item{mu0, eta0, T, M}{Huber estimator parameters.}

\item{k_min_star, k_max_star, include_zero_bin}{Huber scale-estimation parameters.}

\item{m2_frac}{Huber privacy split parameter.}

\item{step_schedule}{Huber GD step schedule.}

\item{dp_v_flag}{Logical; whether to privatize the PCA basis.}

\item{cpp.option}{Logical passed to \code{mech_tau_sph()}.}

\item{mono}{Logical; whether to apply monotone postprocessing.}

\item{type}{Either \code{"lambda"} or \code{"evr"}.}

\item{show_nonprivate}{Logical; whether to overlay nonprivate scree.}

\item{show_points}{Logical; whether to add points.}

\item{col_map}{Named color vector. Defaults are used when \code{NULL}.}

\item{lty_map}{Named line-type vector. Defaults are used when \code{NULL}.}

\item{pch_map}{Named point-shape vector. Defaults are used when \code{NULL}.}

\item{main, xlab, ylab, ylim}{Plot controls.}

\item{legend_pos}{Legend position.}

\item{...}{Additional graphical arguments passed to \code{plot()}.}
}
\value{
Invisibly returns a list containing nonprivate results and
method-specific \code{dp_scree()} outputs.
}
\description{
User-facing function that computes and plots nonprivate and/or differentially
private scree curves using one or more DP eigenvalue estimators.
}
