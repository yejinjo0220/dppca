% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pc_dir.R
\name{dp_pc_dir}
\alias{dp_pc_dir}
\title{Estimate principal component directions}
\usage{
dp_pc_dir(
  X,
  k,
  center = TRUE,
  standardize = FALSE,
  g_dppca = FALSE,
  eps = NULL,
  delta = NULL,
  cpp.option = FALSE
)
}
\arguments{
\item{X}{A numeric matrix or data frame. Rows correspond to observations and
columns correspond to variables.}

\item{k}{Number of principal component directions to return. Must be an
integer between \code{1} and the number of columns in \code{X}.}

\item{center}{A logical value indicating whether to center the columns of \code{X}
before computing principal component directions. The default is \code{TRUE}.}

\item{standardize}{A logical value indicating whether to scale the columns of
\code{X} by their sample standard deviations after optional centering. The
default is \code{FALSE}.}

\item{g_dppca}{Whether to compute private principal component directions using
the spherical Kendall mechanism based on the g-DPPCA method. If \code{FALSE},
the usual non-private directions are computed from the sample covariance
matrix. The default is \code{FALSE}.}

\item{eps}{Positive number defining the \code{epsilon} privacy parameter for
private principal component directions. Required when \code{g_dppca = TRUE}.}

\item{delta}{Number in \verb{(0, 1)} defining the \code{delta} privacy parameter for
private principal component directions. Required when \code{g_dppca = TRUE}.}

\item{cpp.option}{A logical value reserved for a future C++ implementation of
the spherical Kendall matrix. Currently only \code{FALSE} is supported.}
}
\value{
A numeric matrix with \code{ncol(X)} rows and \code{k} columns. The columns are
orthonormal principal component directions.
}
\description{
This function returns the principal component directions used by the plotting
and estimation functions in this package. By default, it computes the usual
non-private directions from the sample covariance matrix. If \code{g_dppca = TRUE},
it computes private directions using the spherical-transformation version of
g-DPPCA proposed by \insertCite{kim2025robustdppca;textual}{dppca}: it adds
Gaussian noise to the spherical Kendall matrix and then extracts its leading
eigenvectors.
}
\details{
The non-private option computes leading eigenvectors of the sample covariance
matrix of the preprocessed data. The private option is based on the spherical
Kendall mechanism of \insertCite{kim2025robustdppca;textual}{dppca}: it first
forms the spherical Kendall matrix from pairwise normalized differences, adds
symmetric Gaussian noise, and then computes leading eigenvectors. The final
eigenvector matrix is re-orthonormalized by QR decomposition.
For a detailed procedure and mathematical formulations,
refer \url{https://yejinjo0220.github.io/dppca/articles/pc_direction}.
}
\examples{
data(gau, package = "dppca")

# Use a small subset to keep the example fast.
X <- gau[1:200, ]

# Non-private principal component directions
V <- dp_pc_dir(X, k = 2)
head(V)


# Private principal component directions
set.seed(123)
V_private <- dp_pc_dir(
  X,
  k = 2,
  g_dppca = TRUE,
  eps = 2,
  delta = 1e-3
)
head(V_private)

}
\references{
\insertRef{kim2025robustdppca}{dppca}
}
