% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_scree.R
\name{dp_scree_pmwm}
\alias{dp_scree_pmwm}
\title{Differentially private scree estimation via PMWM}
\usage{
dp_scree_pmwm(
  X,
  k,
  eps_total,
  delta_total,
  g_dppca = FALSE,
  cpp.option = FALSE,
  split_mode = TRUE,
  center = TRUE,
  standardize = TRUE,
  beta = 1.01,
  a,
  b,
  trim_const = 10,
  eta = 0.01,
  mono = TRUE,
  max_extra_bins = 1000
)
}
\arguments{
\item{X}{Numeric data matrix with observations in rows.}

\item{k}{Integer number of leading principal components.}

\item{eps_total}{Total privacy epsilon allocated to the scree routine.}

\item{delta_total}{Total privacy delta allocated to the scree routine.}

\item{g_dppca}{Logical; whether to privatize the PCA direction matrix.}

\item{cpp.option}{Logical passed to \code{mech_tau_sph()} when private
directions are computed.}

\item{split_mode}{Logical; if \code{TRUE}, split the sample into quantile and
mean subsets.}

\item{center}{Logical; whether to center columns before PCA.}

\item{standardize}{Logical; whether to standardize columns before PCA.}

\item{beta}{Log-binning base used in the private quantile estimator.}

\item{a}{Finite lower support bound supplied to the private quantile routine.}

\item{b}{Finite upper support bound supplied to the private quantile routine.}

\item{trim_const}{Positive constant controlling the practical clipping
proportion \code{max(trim_const / n_q, eta)}.}

\item{eta}{Lower bound in the practical clipping proportion.}

\item{mono}{Logical; whether to post-process the final scree vector so that
it is nonnegative and nonincreasing.}

\item{max_extra_bins}{Nonnegative integer controlling how far the unbounded
quantile scan extends past the largest occupied bin.}
}
\value{
A list with components:
\describe{
\item{\code{scree}}{Private scree estimates.}
\item{\code{scree_np}}{Non-private winsorized scree estimates.}
\item{\code{evr}}{Explained variance ratios computed from the private
scree estimates.}
}
}
\description{
Internal helper that estimates the leading scree values using a PMWM-style
procedure based on:
\enumerate{
\item PCA preprocessing and direction estimation,
\item centered squared scores for each component,
\item private lower and upper quantile estimation,
\item winsorization of the squared scores,
\item Gaussian release of the winsorized mean,
\item the \eqn{n/(n-1)} scree correction.
}

If \code{split_mode = TRUE}, one subset of the sample is used for private
quantile estimation and the other subset is used for the winsorized mean step.
If \code{split_mode = FALSE}, the full sample is reused in both steps.

The clipping proportion is the practical choice
\code{max(trim_const / n_q, eta)}, truncated above at \code{0.49}.
}
\keyword{internal}
