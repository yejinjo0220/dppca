% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_score.R
\name{dp_frame}
\alias{dp_frame}
\title{Plotting frame for 2D scores}
\usage{
dp_frame(
  X,
  eps_q = NULL,
  delta_q = NULL,
  inflate = 0.1,
  q = NULL,
  frame = NULL
)
}
\arguments{
\item{X}{Numeric matrix with exactly two columns.}

\item{eps_q}{Privacy budget for DP quantile estimation. Required only when
\code{frame = NULL}.}

\item{delta_q}{Privacy parameter for DP quantile estimation. Required only
when \code{frame = NULL}.}

\item{inflate}{Non-negative numeric value controlling frame expansion.}

\item{q}{Optional symmetric quantile level in \eqn{(0, 1)}. If \code{NULL},
extreme order statistics are used.}

\item{frame}{Optional user-specified frame. This can be either a numeric
vector of length 2, interpreted as a common range for both axes, or a list
with components \code{xlim} and \code{ylim}.}
}
\value{
A list with components \code{xlim} and \code{ylim}.
}
\description{
Internal helper that constructs a plotting frame for two-dimensional score
data. If \code{frame} is supplied, it is used directly. Otherwise a
differentially private frame is constructed from privatized lower and upper
quantiles computed from the pooled score coordinates.
}
\keyword{internal}
