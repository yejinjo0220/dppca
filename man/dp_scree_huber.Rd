% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_scree.R
\name{dp_scree_huber}
\alias{dp_scree_huber}
\title{Differentially private scree estimation via Huber-type robust mean}
\usage{
dp_scree_huber(
  X,
  k,
  eps_total,
  delta_total,
  g_dppca = FALSE,
  cpp.option = FALSE,
  center = TRUE,
  standardize = FALSE,
  mu0 = 0,
  eta0 = 1,
  T = NULL,
  M = NULL,
  k_min_m2 = -20,
  k_max_m2 = 40,
  m2_frac = 1/4,
  mono = TRUE
)
}
\arguments{
\item{X}{Numeric data matrix with observations in rows.}

\item{k}{Integer number of leading principal components.}

\item{eps_total}{Total privacy epsilon allocated to the scree routine.}

\item{delta_total}{Total privacy delta allocated to the scree routine.}

\item{g_dppca}{Logical; whether to privatize the PCA direction matrix.}

\item{cpp.option}{Logical passed to \code{mech_tau_sph()} when private
directions are computed.}

\item{center}{Logical; whether to center columns before PCA.}

\item{standardize}{Logical; whether to standardize columns before PCA.}

\item{mu0}{Initial value for noisy gradient descent.}

\item{eta0}{Fixed step size used in noisy gradient descent.}

\item{T}{Optional integer number of gradient descent iterations. If
\code{NULL}, a default based on \code{ceiling(log(n))} is used.}

\item{M}{Optional integer number of blocks used in \code{dp_m2()}. If
\code{NULL}, a default based on \code{floor(sqrt(n / 2))} is used.}

\item{k_min_m2}{Integer lower bound for dyadic histogram bins used in
\code{dp_hist_m2()}.}

\item{k_max_m2}{Integer upper bound for dyadic histogram bins used in
\code{dp_hist_m2()}.}

\item{m2_frac}{Fraction of the scree privacy budget allocated to the private
scale-proxy step \code{dp_m2()}.}

\item{mono}{Logical; whether to post-process the final scree vector so that
it is nonnegative and nonincreasing.}
}
\value{
A list with components:
\describe{
\item{\code{scree}}{Private scree estimates.}
\item{\code{scree_np}}{Non-private scree estimates computed as
\eqn{(n/(n-1)) \times mean(w_{i\ell})}.}
\item{\code{pve}}{Proportions of variance explained computed from the private scree
estimates.}
}
}
\description{
Internal helper that estimates the leading scree values using a Huber-type
differentially private mean estimator applied to the centered squared scores
of each principal component.

The procedure is as follows:
\enumerate{
\item preprocess the data matrix for PCA,
\item compute a non-private or private direction matrix,
\item form the projected score matrix,
\item for each component \eqn{\ell}, compute centered squared scores
\eqn{w_{i\ell} = (y_{i\ell} - \bar y_\ell)^2},
\item privately estimate a scale proxy from \eqn{w_{1\ell}, \dots, w_{n\ell}}
using \code{dp_m2()},
\item convert that scale proxy into a Huber threshold using
\code{tau_from_m2()},
\item run noisy gradient descent via \code{dp_huber_noisy_gd()} to obtain
a private mean estimate of \eqn{w_{i\ell}},
\item apply the \eqn{n/(n-1)} correction to obtain the final scree estimate.
}

If \code{mono = TRUE}, the final scree vector is post-processed so that it is
nonnegative and nonincreasing.
}
\keyword{internal}
