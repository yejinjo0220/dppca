% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score.R
\name{dp_score_plot}
\alias{dp_score_plot}
\title{Plot differentially private score histograms or sampled score points}
\usage{
dp_score_plot(
  X,
  privacy,
  bins,
  method = c("add", "sparse"),
  private_plot = c("histogram", "sample"),
  sampling_control = NULL,
  center = TRUE,
  standardize = FALSE,
  cpp.option = FALSE,
  axes = c(1, 2),
  plot_control = NULL,
  V_dp = NULL
)
}
\arguments{
\item{X}{A numeric matrix or data frame. Rows correspond to observations and
columns correspond to variables.}

\item{privacy}{Output of \code{\link[=privacy_control]{privacy_control()}} with exactly \code{loading} and
\code{score} components. For example, use \code{privacy_control(eps = 3, delta = 1e-4, split = c(loading = 0.5, score = 0.5))}. The loading
allocation is used once when \code{V_dp = NULL}. With supplied directions,
it records the budget already used to obtain them and is not spent again
or reassigned. The histogram allocation is supplied separately to each
requested histogram method; see Details.}

\item{bins}{Integer vector of length 2 defining the number of histogram bins
along the first and second score axes, respectively.}

\item{method}{Character vector specifying which private histogram methods to
compute. Use \code{"add"} for the additive Gaussian histogram and \code{"sparse"} for
the sparse thresholded histogram. The default is \code{c("add", "sparse")}.}

\item{private_plot}{Character vector controlling how requested histogram
results are visualized. \code{"histogram"} displays the histogram panels,
while \code{"sample"} displays synthetic score points sampled from the
histograms. Both can be requested with
\code{c("histogram", "sample")}. The default is
\code{c("histogram", "sample")}.}

\item{sampling_control}{Optional control list created by
\code{sampling_control()}. It is used only when \code{"sample"} is included in
\code{private_plot}. If \code{NULL}, the default sampling settings are used.}

\item{center}{A logical value indicating whether to center the columns of \code{X}
before projection and any direction estimation. The default is \code{TRUE}.}

\item{standardize}{A logical value indicating whether to scale the columns of
\code{X} by their sample standard deviations after optional centering. The
default is \code{FALSE}.}

\item{cpp.option}{A logical value passed to \code{\link[=dp_loading]{dp_loading()}} to select the
Rcpp spherical-Kendall implementation. The default is \code{FALSE}.}

\item{axes}{Positive integer vector of length 2 specifying the principal
components used as the horizontal and vertical score axes, in that order.
Each value must not exceed \code{ncol(X)}. Repeated or decreasing indices are
accepted. The default is \code{c(1, 2)}.}

\item{plot_control}{Optional control list created by \code{score_plot_control()}.
If \code{NULL}, default score-plot settings are used.}

\item{V_dp}{Optional finite real \code{p} by \code{p} private direction matrix, where
\code{p} is the number of feature columns. Columns must be orthonormal and rows
must follow the feature order in \code{X}; supplied row names must match its
column names when both are present. Directions must use the same
preprocessing as this call. If supplied, direction estimation is skipped.
The caller is responsible for the matrix's DP provenance and prior budget;
matrix validation does not establish either.}
}
\value{
A named list containing:
\describe{
\item{score}{The output of \code{\link[=dp_score]{dp_score()}}.}
\item{sample}{Present only when \code{"sample"} is requested. A nested list
containing sampled coordinates for each requested private histogram
method (\code{add} and/or \code{sparse}). Each leaf is a data frame with columns
\code{pc_x} and \code{pc_y}, indexed by sampling method (\code{center} and/or \code{uniform}).}
\item{plot}{A list containing the original scatter plot, the non-private
histogram plot, requested private histogram plots, sampled-score plots,
and the combined patchwork layout in \code{plot$all}. Sampled-score plots are
stored under \code{plot$sample}.}
}
}
\description{
This function computes two-dimensional principal component score histograms
and visualizes the results. Private histogram releases can be shown directly
as histogram panels or post-processed into synthetic scatter plots sampled
from the released histograms.
}
\details{
Histogram plots and sampled-score plots are arranged dynamically. When only
\code{"histogram"} is requested, the original scatter plot is shown first,
followed by the non-private and requested private histograms. When
\code{"sample"} is requested, the layout uses one reference column followed by
one column for each requested private method. The original scatter plot
starts the first sample row, and later sample rows use a blank reference
panel. If both plot types are requested, the non-private histogram starts
the histogram row.

Synthetic score sampling is applied only to the requested private
histograms. The original scatter plot and non-private histogram are retained
as references, but no synthetic samples are drawn from the non-private
histogram.

This comparison wrapper calls \code{\link[=dp_score]{dp_score()}} with \code{non_private = TRUE} and
has no \code{non_private} argument. Its observed scatter and empirical histogram
use the supplied or estimated private directions, not ordinary PCA directions;
these panels and their returned data are non-private references.

Each wrapper call computes a new frame and new histograms, even with supplied
\code{V_dp}. To redraw stored estimates without repeating those releases, use
\code{\link[=dp_pca]{dp_pca()}} with \code{non_private = FALSE} and \code{\link[=plot.dp_pca]{plot.dp_pca()}}.
}
\examples{
data(gau, package = "dppca")

X <- gau[1:300, ]

# By default, both histogram and sampled panels are displayed.
set.seed(123)
score_plot <- dp_score_plot(
  X,
  privacy = privacy_control(
    eps = 3, delta = 1e-3,
    split = c(loading = 0.5, score = 0.5)
  ),
  bins = c(8, 8),
  method = "add"
)
score_plot$plot$all

# Sample-only display with two sampling methods.
set.seed(123)
sample_plot <- dp_score_plot(
  X,
  privacy = privacy_control(
    eps = 3, delta = 1e-3,
    split = c(loading = 0.5, score = 0.5)
  ),
  bins = c(8, 8),
  method = "add",
  private_plot = "sample",
  sampling_control = sampling_control(
    method = c("center", "uniform")
  )
)
sample_plot$plot$all

}
\seealso{
\code{\link[=dp_score]{dp_score()}} for computing score histograms without plotting.
\code{\link[=dp_score_plot_group]{dp_score_plot_group()}} for group-wise score histogram plots.
}
