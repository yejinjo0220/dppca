% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loading.R
\name{dp_loading_plot}
\alias{dp_loading_plot}
\title{Build loading plots directly from data}
\usage{
dp_loading_plot(
  X,
  eps,
  delta,
  axes = c(1, 2),
  display = "vector",
  heatmap_components = NULL,
  center = TRUE,
  standardize = FALSE,
  cpp.option = TRUE,
  variables = 10,
  labels = TRUE,
  align_sign = TRUE,
  plot_control = NULL
)
}
\arguments{
\item{X}{Numeric matrix or data frame; rows are observations.}

\item{eps}{Positive privacy parameter epsilon.}

\item{delta}{Privacy parameter delta in \verb{(0, 1)}.}

\item{axes}{Two distinct PCs for vector and point displays.}

\item{display}{One or more of \code{"vector"}, \code{"point"}, \code{"heatmap"}.}

\item{heatmap_components}{PCs in the heatmap; \code{NULL} uses \code{axes}.}

\item{center}{Whether to center columns. Default: \code{TRUE}.}

\item{standardize}{Whether to divide columns by their sample standard
deviations. Default: \code{FALSE}.}

\item{cpp.option}{Use the compiled spherical Kendall implementation from
dppca. Default: \code{TRUE}. Use \code{FALSE} for the R implementation in this file.}

\item{variables}{A positive number selects top variables separately in each
panel. Use \code{"all"} or \code{NULL}, variable names, or multiple column indices
for other selections. Default: top 10.}

\item{labels}{Show variable labels in vector and point displays.}

\item{align_sign}{Align only non-private plotting directions to the private
directions. Default: \code{TRUE}. Returned loading matrices are unchanged.}

\item{plot_control}{Optional output of \code{\link[=loading_plot_control]{loading_plot_control()}}.}
}
\value{
A list with \code{loading} (the result of \code{\link[=dp_loading]{dp_loading()}}) and \code{plot}
(display-named \code{nonprivate} and \code{private} lists, plus \code{all}). Print
\code{result$plot$all} to draw the combined patchwork.
}
\description{
Build loading plots directly from data
}
\details{
One display has non-private left and private right. Multiple
displays have non-private above private, ordered vector, point, heatmap.
Non-private panels and comparison-based plot ranges are reference output;
the combined result is not a private-only release.
}
\examples{
\dontrun{
set.seed(123)
X <- matrix(rnorm(150), 50, 3)
out <- dp_loading_plot(X, eps = 2, delta = 1e-3, cpp.option = FALSE)
out$loading$nonprivate
out$loading$private
out$plot$all
}
}
